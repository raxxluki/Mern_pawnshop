const Input = ({type, className, placeholder, defaultValue}) => {
    return <input
        type={type}
        className={className}
        placeholder={placeholder}
        defaultValue={defaultValue}/>
}

export default Input;