import {accountBaseUrl, default as accountRequestUrls} from "../account/requestUrls";

export const apiBaseUrl = "/api";

const requestUrls = {
    apiBaseUrl,
    accountBaseUrl,
    accountRequestUrls
}

export default requestUrls;